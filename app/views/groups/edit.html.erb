<h1> Invite a new user to <%= @group.name %> </h1>

<%= form_for @invite , url: invites_path do |f| %>
  <%= f.hidden_field :group_id, value: @invite.group_id %>
  <%= f.label :email %>
  <%= f.email_field :email, required: true %>
  <%= f.submit %>
<% end %>

